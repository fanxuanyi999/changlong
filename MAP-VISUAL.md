# Day2 地图 · 手绘三层版

本次在已有 Day2 组件上迭代，沿用主站入口 `index.html#day2-map`，无独立 demo、无新增地图 SDK。

## 审查与拆层

原版已经把插画放在独立文件，但根 SVG 内的底图 image、路线、节点、设施、位置标记是同级元素，职责没有形成明确容器。本版只增加分层容器与呈现状态，保留既有事件委托、路径算法、选择器和控件。

| 层 | 容器 | 职责 |
| --- | --- | --- |
| Illustration Base | `.pm-illustration-base` | 独立的 `assets/park-illustration.svg`；承载水彩树林、湖岸、水纹、地形、步道、建筑及动物场景 |
| Navigation Overlay | `.pm-navigation-overlay` | 原 `.pm-paths` 与 `.pm-you`；主线、方向箭头、完成状态、临时引导与已校准蓝点 |
| Interactive POI | `.pm-interactive-poi` | 原 `.pm-nodes` 与 `.pm-facilities`；景点编号、名称、时间和互斥设施图层 |

Bottom Sheet 是独立 DOM 功能面板，用 `data-map-ui="poi-details"` 标识，保持原有非模态操作逻辑。底图与导航容器不接收指针事件，触摸由原 SVG 和 POI 处理。

## 一个坐标系

沿用 **1200 × 900 normalized logical coordinates**。它是任意逻辑单位的共享坐标框架，不是经纬度或屏幕像素。与改成 1000 × 1000 相比，保留原始比例可避免变形与坐标迁移。

- 三个容器均位于同一个 `.pm-map` 根 SVG 内，不设置各自的 viewBox 或 CSS 定位。
- 底图固定映射 `(0,0) → (1200,900)`，原始绘画比例也是 4:3。
- 12 个节点、11 条路段、17 个设施继续读取 `park-map-data.js`，该文件逐字节保持不变。
- 缩放、拖动、自动居中和全图均只改变根 SVG 的 viewBox，原 `mapPoint()` 使用根 SVG 的逆变换。
- 木牌和时间文字仅使用相对 POI 的标签偏移；标签的简化不改变点位坐标。

数据文件 SHA-256：`1142959e034b34c174427334368aad9d2517cd96c303d7f2d4445dbe303cf54e`。

## 底图艺术

使用内置 ImageGen，以原有地形、湖泊和道路轮廓图为几何参照，生成整体水彩手绘园区场景。不是将路线或设施图标烘焙进图片。生成图仅作 WebP 格式压缩，未裁切、重新排布点位或二次绘制。绘画以 1448 × 1086 输出，压缩为约 586 KB WebP，再内嵌进 SVG 封装；浏览器只需缓存一个约 782 KB 的底图文件。

提示方向：专业主题乐园手绘导览、自然水彩与细线稿、统一光影、错落树冠、柔和湖水与岸线、米白步道、轻等距建筑、低干扰动物；严格跟随输入参考的地形和空间位置；不生成文字、编号、POI 标记、导航线或箭头。

这是 AI 辅助创作的艺术示意底图，并非长隆官方发布的地图。动物和建筑插画用于识别场景，不代表设施实际入口或实时开放状态。GPS 的既有边界保持不变：定位获取后仍需现场手动确认节点，不宣称自动投影、实时跟随或转弯导航。

## 视觉和轻量补充

- 独立 `park-map-visual.css` 覆盖本组件样式，原 `park-map.css` 保持不变。
- 橙红色主线带浅色边缘，每段一个顺向箭头；蓝色目标引导沿用原算法，设施最后一段保留蓝色虚线。
- 当前节点轻微呼吸、下一站描边、完成节点和路段降低饱和度、目标点蓝色高亮；尊重减少动画设置。
- 按实际 SVG 显示比例分为三个密度：全图编号、区域名称、近景完整名称和时间。餐饮复合名称在区域级缩短，近景恢复全称。
- 面板最多占地图高度 40%，内容可独立滚动；手机画布减少无效留白。
- 双击调用既有缩放方法；“全部关闭”调用既有主线图层操作；详情新增“查看完整路线”，仍复用原完整路线操作。

## 回归记录（2026-09-22）

自动测试：`node --test tests/map.test.cjs tests/map-visual.test.cjs`，9 项通过。覆盖全部 144 个起终点组合、连续性与方向、设施锚点、GPS 成功/拒绝/不可用/超时、PWA 子路径缓存、双指手势、拖动、双击/滚轮锚点和缩放边界。

浏览器检查：

- 手机约 390 CSS px、桌面约 1280 CSS px；三层同画布、编号/名称/时间随缩放切换。
- 12 个节点逐一打开，名称、建议时间、附近设施、自动居中与关闭正常。
- 四类设施互斥：厕所 8、餐饮 3、缆车 3、休息点 3；全部关闭后设施为 0，保留 12 个核心节点。
- 直接点击 SVG、键盘 Enter/Escape、手动校准和清除位置、当前位置/下一站/完成节点状态。
- 北门到熊猫、熊猫返回北门、厕所目标虚线指引；目标引导时没有相向主线箭头叠加。
- 双击放大、拖动、滚轮、缩放控件、恢复全图；近景标签完整可读。
- 手机和桌面 Bottom Sheet 实测高度比例约 0.40。
- 点击保存到离线，显示成功后停止本机 HTTP 服务；重开页面，水彩底图、餐饮图层与设施详情仍可用。

双指手势与 GPS 分支使用自动化模拟验证；尚未做园内 GPS 实测或 iOS 主屏安装实机验证。

## 部署与后续替换

继续使用原 GitHub Pages 根目录。更新 `sw.js` 缓存版本并将视觉 CSS 加入缓存清单；底图内嵌 WebP 不依赖外部图片服务。旧 `offline.html` 仍是原旅行文字/照片快照，互动地图的离线能力由当前 PWA 承担。

以后只需替换 `assets/park-illustration.svg` 内的插画并保持 1200 × 900 映射，即可更换底图；无需调整 POI 或路线。新增设施只改原数据，路线变化也不要求重新制作插画。
