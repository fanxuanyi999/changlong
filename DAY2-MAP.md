# Day2 互动地图

2026-09-22 已升级为独立水彩底图、导航层、交互 POI 三层结构。坐标和原交互保留，艺术与回归记录见 [MAP-VISUAL.md](MAP-VISUAL.md)。

## 接入方式

现有网站是无构建的 HTML / CSS / JavaScript。`app.js` 的 `renderDay()` 渲染每日攻略，新增地图只在 Day2 时间轴前挂载 `<day-two-map>`。三日速览、其他两天、照片、行李清单与旅行备忘保持原结构。直接入口为 `index.html#day2-map`。

地图没有地图 SDK、付费 API 或外部运行时依赖。`park-map-data.js` 存放12个节点、11条有向路段、17个区域设施参考点；`park-map.js` 负责交互；`park-map.css` 所有选择器限制在模块内；`assets/park-illustration.svg` 是独立简化插画底图。

## 已实现

- 默认仅开启主线与12个节点。路径与箭头共用同一有序点列，每段只有一个顺向箭头。
- 节点可鼠标、触屏、键盘或下拉选择；自动居中并弹出非模态 Bottom Sheet，展示计划时间、停留、下一站及附近设施参考。
- 厕所 / 餐饮 / 缆车 / 休息点互斥筛选，再点当前图层即收起。附近设施按钮使用同一份数据。
- 手动“我在这里”与节点选择校准；蓝色标记明确标注已校准。本机只保存节点 ID 和确认时间，6小时后重新确认。
- 起点到目标按有序主线引导，返回目标时仅渲染反向蓝色箭头，隐藏原主线箭头；不出现相向箭头叠加。设施最后一段用区域指向虚线，不冒充核实步道。
- 拖拽、鼠标滚轮、双指缩放、缩放按钮和恢复全图。组件移除时清理观察器与未完成定位回调。
- 浏览器定位主动授权，超时、拒绝和不可用状态可恢复；GPS结果仅存在内存、不上传。
- 15:00–15:30缆车可选休息观景，选择缆车需删减同期步行。根据当天线路、上下站与总用时决定，不默认南门；15:30开始收尾。
- PWA Manifest、主屏图标、Service Worker、显式离线保存与成功/失败反馈。首次成功缓存后，在原路径离线重开可用；保留原独立文字攻略 `offline.html`。

## 地理数据边界

参考源：长隆野生动物世界官网 https://www.chimelong.com/gz/safaripark/ ，网页内的官方插画导览图 https://cdn.chimelong.com/upload/11aec41545b0408e/5f527c8de2142284.png ，于2026-09-22读取。

底图是空间关系转绘，点位坐标为插画单位；设施为区域级参考，尚未核验精确入口与最新开放情况。休息点表示建议休息区域，不声明具体座椅存在。附近设施按插画距离排序，没有伪造实际步行米数或时间。体力判断点为行程节点，不是园区设施。

**第一版不提供自动GPS蓝点投影或实时转弯导航。** 官方插画未提供可用的测绘坐标、控制点与通行路网。GPS获取成功后，请在现场确认节点；显示的“你在这里”是手动校准点，移动后需更新。没有将 WGS84 / GCJ02 混用，也没有用编造的园区边界线性映射GPS。

下一步若需要自动跟随，须采集可信的经纬度与图像控制点，验证插画局部形变及误差，再接入受校准精度约束的投影；有经核实路网后才加入距离与最短路线。当前版可作为交互与节点路书使用，不应作为精确园区导航。

## 验证

运行 `node --test tests/map.test.cjs`。测试覆盖全部144种起终点组合、反向路径无数据污染、设施引用、GPS成功与三类失败、GitHub Pages子路径缓存、离线资源与站外请求隔离。

浏览器实测：手机约392 CSS像素，12个节点逐一打开，直接点击SVG点位，厕所切餐饮互斥，手动校准、前進/返回引导；停止本机HTTP服务后重载，地图、厕所筛选和详情仍可用。精确GPS现场误差与iOS主屏安装未做实机验证。

部署仍沿用仓库根目录 GitHub Pages，无需构建。缓存更新时修改 `sw.js` 的 `VERSION`；仅清理本站自己的旧缓存。原离线HTML是既有文字攻略快照，互动地图离线通过PWA实现，两者入口已在页面说明。
